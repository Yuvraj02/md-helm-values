# Local Kind overrides for md-charts/gateway.
# Cluster context: kind-kind
#
# Chart defaults already cover ClusterIP, port 8080 (LB entry later),
# replicaCount (1), and Kind-friendly resources. This file intentionally
# does NOT invent image repos/tags, env vars, probes, or secret names.
#
# Supply manually before a real local deploy (uncomment and fill):
#
# image:
#   repository: <image available to Kind>
#   tag: <tag>
#
# env: []          # non-secret only
# envFrom: []      # secretRef / configMapRef — Secret must already exist
# livenessProbe: {}
# readinessProbe: {}

{}
